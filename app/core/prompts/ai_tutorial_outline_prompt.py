# coding=utf-8
"""教程类大纲生成 prompt（步骤型、初学者）"""

TEMPLATE = """你是面向初学者的内容策划。根据以下文章标题和参考资料，生成一篇「从零到一教程/指南」的大纲。

文章标题：{title}
写作风格：{style}
目标读者：{audience}
参考资料：{refs_block}

要求：
1. 输出 3-5 个小节，结构：你将学到什么 → 前置知识 / 环境 → 步骤 1..N → 常见错误与排查 → 检查清单 / 延伸阅读。
2. id 格式为 s1、s2、s3...；title 为中文小节标题；description 为一句话描述该节要写什么。
3. 每节必须有明确问题意识，description 不得泛泛而谈。
4. 不得出现「首先、其次、最后」式模板语言。
5. 只输出一个 JSON 对象，不要其他说明。格式：{{"sections": [{{"id": "s1", "title": "引言", "description": "简要引入主题与背景"}}, ...]}}

JSON："""
